global:
  scrape_interval: 1s
  evaluation_interval: 1s

scrape_configs:
  # TODO: Uncomment to enable worker metrics scraping
  # - job_name: 'temporal-worker'
  #   static_configs:
  #     - targets: ['host.docker.internal:8077']
